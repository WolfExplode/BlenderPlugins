# Bpaint

Bbrush-style secondary brush for Texture Paint (Blender 5.1+).

- Hold **Shift** in Texture Paint mode: the active brush switches to the secondary brush (Blur by default).
- Release **Shift**: the previous brush is restored.
- To change the secondary brush, hold **Shift** and click another brush in the asset shelf. It is remembered for the session.
